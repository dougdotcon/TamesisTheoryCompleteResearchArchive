# Sessão LAB-0.11 — disponibilidade do cache

## Escopo

Auditoria somente leitura da matriz de containers, marcadores, hashes críticos e reprodutibilidade do par Lean/Mathlib. Nenhum benchmark Clay ou RH foi iniciado.

## Execuções

1. Launcher `lean-env.ps1` corrigido para encaminhar argumentos; Lean 4.32.2 e cURL 8.21.0 confirmados.
2. `cache query HEAD`: marcador ausente para o HEAD corrente.
3. Probes seriais HTTPS dos dois hashes em `master` e `legacy`.
4. Script read-only `10_TOOLS/audit_cache_objects.py`: 398 `.ltar`, 2.583 `.part`.
5. Tentativa de instalação isolada `leanprover/lean4:v4.32.1`: timeout; smoke não executado.

## Fechamento

Classificação: `LAB011_NO_CACHE_BACKED_PAIR_FOUND`. A recuperação autorizada permanece limitada à seleção de container oficial e à repetição do probe isolado; não há base para promover a evidência a C/P.
