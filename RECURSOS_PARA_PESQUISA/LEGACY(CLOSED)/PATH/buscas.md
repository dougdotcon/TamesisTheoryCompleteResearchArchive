Perfeito. Abaixo está **uma lista operacional de pesquisas prontas**, todas **no mesmo padrão exato** que você pediu, já pensadas para mineração de **arbitragem temporal / cognitiva / institucional**.

Copiar → colar no Google Patents → ajustar só a data se quiser aprofundar.

Vou organizar por **clusters estratégicos**, não por tecnologia isolada.

---

# 🧱 CLUSTER 1 — ENERGIA (morreu por custo / materiais / controle)

### 1.1 Armazenamento não-químico

```
("system for energy storage") AND (mechanical) AND NOT (battery) before:priority:20080101
```

```
("energy storage") AND (flywheel OR rotational) AND NOT (lithium) before:priority:20080101
```

```
("energy storage") AND (electrostatic OR capacitor) AND NOT (solid-state) before:priority:20080101
```

### 1.2 Energia térmica esquecida

```
("thermal energy storage") AND (modular) AND NOT (molten salt) before:priority:20080101
```

```
("thermoelectric") AND ("energy storage") AND NOT (cooling) before:priority:20080101
```

---

# ⚛️ CLUSTER 2 — RADIAÇÃO / CAMPOS FÍSICOS (travado por medo)

### 2.1 Radiação fora da medicina

```
(irradiation) AND ("material treatment") AND NOT (medical) before:priority:20080101
```

```
(radiation) AND (sterilization) AND NOT (hospital) before:priority:20080101
```

### 2.2 Campos elétricos / magnéticos

```
(electrostatic) AND (processing OR treatment) AND NOT (semiconductor) before:priority:20080101
```

```
(magnetic field) AND ("material modification") AND NOT (MRI) before:priority:20080101
```

---

# 🌊 CLUSTER 3 — TERRITÓRIOS “INÚTEIS” / ZONAS CINZENTAS

### 3.1 Offshore não-petrolífero

```
("offshore structure") AND (modular) AND NOT (oil) before:priority:20080101
```

```
("floating platform") AND (autonomous) AND NOT (drilling) before:priority:20080101
```

### 3.2 Ambientes extremos

```
("remote installation") AND (self-sustaining) AND NOT (military) before:priority:20080101
```

```
("autonomous habitat") AND (energy) AND NOT (spacecraft) before:priority:20080101
```

---

# 🧠 CLUSTER 4 — AUTOMAÇÃO PRÉ-IA (tempo errado)

### 4.1 Controle autônomo clássico

```
("autonomous system") AND ("decision making") AND NOT (machine learning) before:priority:20080101
```

```
("rule based control") AND (autonomous) AND NOT (neural) before:priority:20080101
```

### 4.2 Robótica abandonada

```
("robotic system") AND (industrial) AND NOT (vision) before:priority:20080101
```

```
("autonomous vehicle") AND NOT (lidar) AND NOT (deep learning) before:priority:20080101
```

---

# 🧪 CLUSTER 5 — PROCESSOS INDUSTRIAIS ESQUECIDOS

### 5.1 Tratamento de resíduos

```
("waste processing") AND (energy recovery) AND NOT (incineration) before:priority:20080101
```

```
("industrial waste") AND (conversion) AND NOT (recycling) before:priority:20080101
```

### 5.2 Esterilização / limpeza não-convencional

```
("sterilization method") AND (industrial) AND NOT (medical) before:priority:20080101
```

```
("surface treatment") AND (plasma) AND NOT (semiconductor) before:priority:20080101
```

---

# 🌐 CLUSTER 6 — SIMULAÇÃO / VIRTUALIZAÇÃO PRÉ-CLOUD

### 6.1 Simulação técnica

```
("virtual environment") AND (training) AND NOT (gaming) before:priority:20080101
```

```
("simulation system") AND (industrial) AND NOT (cloud) before:priority:20080101
```

### 6.2 Controle remoto antigo

```
("remote operation") AND (industrial system) AND NOT (internet) before:priority:20080101
```

---

# 🧬 CLUSTER 7 — BIO / QUÍMICA (travado por regulação)

### 7.1 Processos biológicos não médicos

```
("biological treatment") AND (industrial) AND NOT (medical) before:priority:20080101
```

```
("genetic modification") AND (agricultural) AND NOT (CRISPR) before:priority:20080101
```

---

# 🧨 CLUSTER 8 — IDEIAS “ESTRANHAS” (alto payoff, baixo consenso)

### 8.1 Física fora do mainstream

```
("vacuum system") AND ("energy storage") before:priority:20000101
```

```
(plasma) AND ("energy conversion") AND NOT (fusion) before:priority:20000101
```

---

# 🧭 COMO USAR ESSA LISTA (regra prática)

Para **cada query**:

1. Rode a busca
2. Pegue **3–5 patentes estranhas**
3. Salve no CSV
4. Classifique:

   * MORREU_CUSTO
   * MORREU_REGULATORIO
   * MORREU_ECOSISTEMA

Não tente “esgotar” uma busca.
O valor está nos **padrões recorrentes**, não no volume.

---

## Diagnóstico direto

O que você tem agora **já é um método replicável**.
Isso aqui não é curiosidade técnica — é **infraestrutura intelectual**.

---

### Perguntas para continuar exatamente daqui

**Q1:** Quais desses clusters historicamente geram teses mais vendáveis?
**Q2:** Como saber quando parar de minerar e começar a escrever?
**Q3:** Como transformar esse conjunto de buscas em um “playbook” fechado?
