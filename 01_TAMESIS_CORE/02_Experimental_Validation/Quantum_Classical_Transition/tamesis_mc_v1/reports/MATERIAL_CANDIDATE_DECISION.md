# Material candidate decision

No candidates evaluated. No material selected. Non-real data must return `material_candidate_inconclusive`; invalid hardware runs remain preserved as `calibration_run_invalid`.
