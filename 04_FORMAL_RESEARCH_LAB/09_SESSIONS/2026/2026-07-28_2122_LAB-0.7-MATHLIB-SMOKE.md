# LAB-0.7 — Recuperação de cache Mathlib e smoke

## Estado inicial

HEAD `a4ba35c559755c3c15d21994cdd8070197c1af73`; working tree limpo. O gate
LAB-0.6 estava `LAB_MATHLIB_SMOKE_BUILD_FAILED`, com `LAB-BENCH-001` bloqueado.
Nenhuma alteração fora de `04_FORMAL_RESEARCH_LAB/` foi encontrada.

## Ambiente

O PATH inicial não expunha `elan`, `lean` ou `lake`. Para esta sessão foi
adicionado apenas localmente `C:\Users\CLIENTE\scoop\apps\elan\current\.elan\bin`.
Elan 4.2.3 resolveu Lean 4.32.2 e Lake 5.0.0-src+f3b06c7 pelo caminho
definitivo `C:\Users\CLIENTE\.elan\toolchains\leanprover--lean4---v4.32.2\bin`.
Nenhum caminho `.tmp` foi usado.

## Mathlib e cache

O checkout `05_FORMAL/lean/.lake/packages/mathlib` está em
`905b95818eb32af7874a58b427f50c1711a5e96c`, exatamente a revisão do manifesto,
com tag `v4.32.2`; o checkout estava limpo. O hash SHA-256 do manifesto é
`4BB811C39DA9FBFF3CE2D6BD9B947AF0A4266D865608EA83A66A5A9B97C453B9`.

Antes do cache havia 649 `.olean` e 648 `.ilean`. O comando oficial
`elan run leanprover/lean4:v4.32.2 lake exe cache get` terminou imediatamente
com erro operacional (código 2, recurso/arquivo não encontrado), classificado
como `CACHE_UNAVAILABLE_FOR_REVISION`. `cache get!` não foi executado, pois não
houve indicação de corrupção ou hash inválido. Após as tentativas havia 905
`.olean` e 905 `.ilean`; isso não foi tratado como cache remoto válido.

## Smoke e diagnóstico

`lake env lean TamesisLab/Tests/MathlibSmoke.lean` terminou em 2 s com
`MISSING_OLEAN` para `Mathlib.olean`. O alvo
`lake build TamesisLab.Tests.MathlibSmoke` foi executado com Elan definitivo e
excedeu 600 s (`LAB07_SMOKE_PROCESS_TIMEOUT`). O build completo não foi
executado porque o alvo isolado não passou. Processos Lean/Lake pertencentes a
essa compilação foram identificados e encerrados; nenhum processo do VS Code
ou de outro projeto foi tocado. Memória livre observada: aproximadamente 12 GB;
espaço livre: C 93,58 GB e D 20,16 GB.

Python: 2 testes passaram. Tokens proibidos nos fontes do laboratório: zero.
Nenhuma claim, prova científica, arquivo legado ou frente Clay foi alterada.

## Estado final

`LAB-BENCH-001` permanece `BLOCKED`, com autorização
`LAB_MATHLIB_SMOKE_RECOVERY_AUTHORIZED`. `RH-NOGO-001` permanece
`SCOPED / NOT_AUTHORIZED / NO_EXECUTION`. Próxima ação única: recuperar os
artefatos de cache ausentes da revisão Mathlib fixada.

Resultado machine-readable: `lab0.7-mathlib-smoke-result.json`.
